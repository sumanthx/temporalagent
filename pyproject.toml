[project]
name = "sharepoint-temporal-agent"
version = "0.1.0"
description = "AgentCore SharePoint temporal-query agent"
requires-python = ">=3.11"
dependencies = ["boto3>=1.42,<2"]

[tool.pytest.ini_options]
testpaths = ["tests"]

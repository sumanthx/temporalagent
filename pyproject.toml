[project]
name = "sharepoint-temporal-agent"
version = "0.1.0"
description = "Local vertical slice for a SharePoint temporal-query agent"
requires-python = ">=3.11"

[project.scripts]
temporal-agent = "temporal_agent.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]

